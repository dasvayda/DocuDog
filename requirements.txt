# DocuDog MVP — watch + idle + routing + local report
PyYAML>=6.0
watchdog>=4.0.0
psutil>=5.9.0
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0

# On-device LLM (optional; Windows support may be limited — mock_inference is the default fallback)
# Docs: https://github.com/google-ai-edge/LiteRT-LM | Runtime: https://github.com/google-ai-edge/LiteRT
litert-lm>=0.11.0

# Download gated .litertlm from Hugging Face (optional): pip install "huggingface_hub[cli]"
huggingface_hub>=0.26.0

# MCP server (Cursor / Claude Desktop): python tools/docudog_mcp.py
# Prefer: pip install --user "mcp[cli]"
mcp[cli]>=1.2.0
