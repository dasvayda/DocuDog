# Pivot 1 (260820) 상세 작업서

스펙: [docs/Improvement-Tasks/pivot_1_260820.md](../docs/Improvement-Tasks/pivot_1_260820.md).  
Cursor Plan UI와 같은 에픽. 구현 추적은 [to-do-list.md](../../to-do-list.md) `260820-01`~`260820-05`.

비목표: Shadow Git, 원본 이동, npm, 문서 브라우저 GUI, 벡터 의미검색, OCR PDF.

입구 UX: **MCP / 트레이**. `DocuDog_status.md`는 `.docudog/` 안 옵션 산출물. 강제 오픈 없음.

---

## Stage 1 — `260820-01` `.docudog/` 격리

- 기본 트리: `%USERPROFILE%/.docudog/` (`state`, `report`, audit, activity, status, lineage, lock).
- 워치 루트와 분리. 예전 `Documents/DocuDog/` 가 있고 새 state가 없으면 **복사만** (삭제 없음).
- Features / output-spec: 일상 입구 = MCP.

확인: 워치 폴더에 새 `DocuDog_state.json`이 안 생김.

## Stage 2 — `260820-02` PDF

- `docudog/extract_pdf.py` + pypdf. 텍스트 레이어만. 암호·빈 본문(스캔) skip.
- `_SKIPPABLE_FOR_MVP`에서 `.pdf` 제거.

## Stage 3 — `260820-03` MCP

- `--write-claude-desktop-mcp`, `--write-all-mcp`.
- `docudog_get_lineage`, `docudog_get_context_bundle`.
- search `since`/`until`. SSE는 보류.

`260818-03` 커서 전면 개편은 이 단계에 흡수하지 않음(날짜 필터만). `260805-02` 리소스는 보류 유지.

## Stage 4 — `260820-04` 트레이

- `python main.py --tray` 또는 `tools/docudog_tray.py`.
- 메뉴: MCP 쓰기, `.docudog` 열기, 일시정지. status 강제 오픈 없음.
- P1 분류 시 토스트 1회 (`notify_settings`).

## Stage 5 — `260820-05` 이종 파일 semantic diff

- version/lineage 그룹 직전 vs 최신 본문 델타 → `last_change_summary` / MCP lineage.
