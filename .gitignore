# Secrets
.env
.env.local
.env.*.local
*.pem

# Local runtime config (copy from config.example.json)
config.json
config.json.bak
config.yml
config.yaml

# Machine-local Cursor MCP (copy from .cursor/mcp.json.example)
.cursor/mcp.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# LLM weights — keep outside the repo (C:\my-own-project\local-llm)
models/
*.litertlm
*.gguf
*.safetensors

# Nested third-party clones (each has its own .git; do not add as gitlinks)
reference/

# DocuDog runtime leftovers if generated in-repo
_main_stdout.txt
_main_stderr.txt
out/
DocuDog_state.json
DocuDog_main.lock
classification_report.md
DocuDog_audit_log.md
DocuDog_lineage.md
DocuDog_tag_overrides.json

# OS / IDE
.DS_Store
Thumbs.db
.idea/
*.swp
